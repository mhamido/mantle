package mhamido.mantle.parsers

class PatternTest extends munit.FunSuite {
  // def argPattern(str: String): Pattern =
  //   Parser("<PatternTest>", str).argPattern()
  // def casePattern(str: String): Pattern =
  //   Parser("<PatternTest>", str).casePattern()

  // for (name, f) <- List("arg" -> argPattern, "case" -> casePattern) do {
  //   test(s"$name - var") {
  //     assertEquals(f("x"), Pattern.Var("x"))
  //   }

  //   test(s"$name - con") {
  //     assertEquals(f("X"), Pattern.Apply("X", Nil))
  //   }

  //   test(s"$name - case") {
  //     assertEquals(
  //       f("Some x y z"),
  //       Pattern.Apply("Some", List("x", "y", "z").map(Pattern.Var(_)))
  //     )
  //   }
  // }
}
