module Parity with

mutual {
    fun isEven (n: Int): Bool =
        if n = 0 then true
        else isOdd (n - 1)

    fun isOdd (n: Int): Bool =
        if n = 0 then false
        else isEven (n - 1)
}

fun main () = /* TODO */ ()
