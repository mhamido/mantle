module Pair with

datatype Pair ['a, 'b] = MkPair (/* fst */ 'a, /* snd */ 'b)

fun fst ['a] ['b] (p: Pair['a, 'b]): 'a =
    match p with MkPair (x, _) => x end

fun snd ['a] ['b] (p: Pair['a, 'b]): 'b =
    match p with MkPair (_, x) => x end